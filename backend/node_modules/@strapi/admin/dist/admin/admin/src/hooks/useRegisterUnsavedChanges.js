'use strict';

var React = require('react');
var unsavedChangesRegistry = require('../utils/unsavedChangesRegistry.js');

/**
 * Registers whether the current surface has unsaved edits so session-expiry
 * logout can warn before clearing auth state (which unmounts forms).
 */ const useRegisterUnsavedChanges = (id, enabled)=>{
    React.useEffect(()=>{
        return unsavedChangesRegistry.registerUnsavedChangesCheck(id, ()=>enabled);
    }, [
        id,
        enabled
    ]);
};

exports.useRegisterUnsavedChanges = useRegisterUnsavedChanges;
//# sourceMappingURL=useRegisterUnsavedChanges.js.map

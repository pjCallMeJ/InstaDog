const SEARCHABLE_ATTRIBUTE_TYPES = new Set([
    'string',
    'text',
    'uid',
    'email',
    'enumeration',
    'richtext',
    'biginteger',
    'integer',
    'decimal',
    'float'
]);
// Mirrors the scalar types used by database search without importing server code into the admin.
const applyPrivateSearchDefault = (attribute)=>{
    if (attribute.private === true && attribute.searchable === undefined && typeof attribute.type === 'string' && SEARCHABLE_ATTRIBUTE_TYPES.has(attribute.type)) {
        return {
            ...attribute,
            searchable: false
        };
    }
    return attribute;
};

export { applyPrivateSearchDefault };
//# sourceMappingURL=applyPrivateSearchDefault.mjs.map

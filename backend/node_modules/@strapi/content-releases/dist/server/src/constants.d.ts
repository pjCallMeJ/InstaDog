export declare const RELEASE_MODEL_UID = "plugin::content-releases.release";
export declare const RELEASE_ACTION_MODEL_UID = "plugin::content-releases.release-action";
export declare const ACTIONS: ({
    section: string;
    displayName: string;
    uid: string;
    pluginName: string;
    category?: undefined;
    subCategory?: undefined;
} | {
    uid: string;
    section: string;
    displayName: string;
    category: string;
    subCategory: string;
    pluginName: string;
})[];
export declare const ALLOWED_WEBHOOK_EVENTS: {
    RELEASES_PUBLISH: string;
};
/**
 * Audit Log events, transformed in audit-logs.ts and registered during bootstrap.
 *
 * RELEASE_TRIGGER is different from the releases.publish webhook.
 * The webhook has its own payload format, which is already a public contract.
 *
 * TODO: Decide whether these should eventually converge when the
 * releases.publish payload is revised: replace it with release.trigger,
 * or keep both formats permanently.
 */
export declare const AUDITED_EVENTS: {
    readonly RELEASE_CREATE: "release.create";
    readonly RELEASE_UPDATE: "release.update";
    readonly RELEASE_DELETE: "release.delete";
    readonly RELEASE_TRIGGER: "release.trigger";
    readonly RELEASE_ENTRY_ADD: "release.entry.add";
    readonly RELEASE_ENTRY_UPDATE: "release.entry.update";
    readonly RELEASE_ENTRY_REMOVE: "release.entry.remove";
    readonly RELEASE_SETTINGS_UPDATE: "release.settings.update";
};
//# sourceMappingURL=constants.d.ts.map
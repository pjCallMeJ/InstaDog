'use strict';

var fp = require('lodash/fp');
var utils = require('@strapi/utils');
var index = require('../constants/index.js');
var index$1 = require('../utils/index.js');

const find = (params = {})=>strapi.db.query('plugin::i18n.locale').findMany({
        where: params
    });
const findById = (id)=>strapi.db.query('plugin::i18n.locale').findOne({
        where: {
            id
        }
    });
const findByCode = (code)=>strapi.db.query('plugin::i18n.locale').findOne({
        where: {
            code
        }
    });
const count = (params = {})=>strapi.db.query('plugin::i18n.locale').count({
        where: params
    });
const create = async (locale, { isDefault = false } = {})=>{
    const result = await strapi.db.query('plugin::i18n.locale').create({
        data: locale
    });
    index$1.getService('metrics').sendDidUpdateI18nLocalesEvent();
    if (result?.id) {
        await utils.emitAudit({
            strapi
        }, index.AUDITED_EVENTS.LOCALE_CREATE, {
            localeId: result.id,
            name: result.name,
            code: result.code,
            isDefault
        });
    }
    return result;
};
const update = async (params, updates)=>{
    const previous = await strapi.db.query('plugin::i18n.locale').findOne({
        where: params
    });
    const result = await strapi.db.query('plugin::i18n.locale').update({
        where: params,
        data: updates
    });
    index$1.getService('metrics').sendDidUpdateI18nLocalesEvent();
    if (result && previous?.name !== result.name) {
        await utils.emitAudit({
            strapi
        }, index.AUDITED_EVENTS.LOCALE_UPDATE, {
            localeId: result.id,
            name: result.name,
            code: result.code,
            changes: {
                name: {
                    before: previous?.name ?? null,
                    after: result.name
                }
            }
        });
    }
    return result;
};
const deleteFn = async ({ id })=>{
    const localeToDelete = await findById(id);
    if (localeToDelete) {
        await deleteAllLocalizedEntriesFor({
            locale: localeToDelete.code
        });
        const result = await strapi.db.query('plugin::i18n.locale').delete({
            where: {
                id
            }
        });
        index$1.getService('metrics').sendDidUpdateI18nLocalesEvent();
        await utils.emitAudit({
            strapi
        }, index.AUDITED_EVENTS.LOCALE_DELETE, {
            localeId: localeToDelete.id,
            name: localeToDelete.name,
            code: localeToDelete.code
        });
        return result;
    }
    return localeToDelete;
};
const setDefaultLocale = async ({ code })=>{
    const previousCode = await getDefaultLocale();
    const hasChanged = previousCode !== code;
    // Look up the rows before the write: if this fails afterwards, the default is already
    // switched and nothing recorded it.
    const [previous, next] = hasChanged ? await Promise.all([
        previousCode ? findByCode(previousCode) : null,
        findByCode(code)
    ]) : [
        null,
        null
    ];
    // set() is typed void but resolves to the store row. Returned so callers that used it
    // keep working.
    const result = await index$1.getCoreStore().set({
        key: 'default_locale',
        value: code
    });
    if (hasChanged && next?.id) {
        const before = previousCode ? {
            id: previous?.id ?? null,
            code: previousCode
        } : null;
        await utils.emitAudit({
            strapi
        }, index.AUDITED_EVENTS.LOCALE_DEFAULT_UPDATE, {
            localeId: next.id,
            name: next.name,
            code: next.code,
            changes: {
                defaultLocale: {
                    before,
                    after: {
                        id: next.id,
                        code: next.code
                    }
                }
            }
        });
    }
    return result;
};
const getDefaultLocale = ()=>index$1.getCoreStore().get({
        key: 'default_locale'
    });
const setIsDefault = async (locales)=>{
    if (fp.isNil(locales)) {
        return locales;
    }
    const actualDefault = await getDefaultLocale();
    if (Array.isArray(locales)) {
        return locales.map((locale)=>({
                ...locale,
                isDefault: actualDefault === locale.code
            }));
    }
    // single locale
    return {
        ...locales,
        isDefault: actualDefault === locales.code
    };
};
const initDefaultLocale = async ()=>{
    const existingLocalesNb = await strapi.db.query('plugin::i18n.locale').count();
    if (existingLocalesNb === 0) {
        await create(index.DEFAULT_LOCALE);
        await setDefaultLocale({
            code: index.DEFAULT_LOCALE.code
        });
    }
};
const deleteAllLocalizedEntriesFor = async ({ locale })=>{
    const { isLocalizedContentType } = index$1.getService('content-types');
    const localizedModels = Object.values(strapi.contentTypes).filter(isLocalizedContentType);
    for (const model of localizedModels){
        // FIXME: delete many content & their associations
        await strapi.db.query(model.uid).deleteMany({
            where: {
                locale
            }
        });
    }
};
const locales = ()=>({
        find,
        findById,
        findByCode,
        create,
        update,
        count,
        setDefaultLocale,
        getDefaultLocale,
        setIsDefault,
        delete: deleteFn,
        initDefaultLocale
    });

module.exports = locales;
//# sourceMappingURL=locales.js.map

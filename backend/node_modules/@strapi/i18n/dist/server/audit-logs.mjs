import { AUDITED_EVENTS } from './constants/index.mjs';

const registerAuditEvents = (auditLogsLifecycle)=>{
    const localeResource = (event)=>({
            type: 'locale',
            id: event.localeId,
            name: event.name,
            code: event.code
        });
    auditLogsLifecycle.registerEvent(AUDITED_EVENTS.LOCALE_CREATE, (event)=>({
            resource: localeResource(event),
            details: {
                isDefault: event.isDefault
            }
        }));
    auditLogsLifecycle.registerEvent(AUDITED_EVENTS.LOCALE_UPDATE, (event)=>({
            resource: localeResource(event),
            details: {
                changes: event.changes
            }
        }));
    auditLogsLifecycle.registerEvent(AUDITED_EVENTS.LOCALE_DELETE, (event)=>({
            resource: localeResource(event)
        }));
    /**
   * The resource is the locale that became the default. `before` is null when no
   * default was set yet.
   */ auditLogsLifecycle.registerEvent(AUDITED_EVENTS.LOCALE_DEFAULT_UPDATE, (event)=>({
            resource: localeResource(event),
            details: {
                changes: event.changes
            }
        }));
};

export { registerAuditEvents };
//# sourceMappingURL=audit-logs.mjs.map

'use strict';

/** Only the materialized `path` is compared, so any row carrying one can be passed. */ const isFolderOrChild = (folderOrChild, folder)=>folderOrChild.path === folder.path || folderOrChild.path.startsWith(`${folder.path}/`);

exports.isFolderOrChild = isFolderOrChild;
//# sourceMappingURL=folders.js.map

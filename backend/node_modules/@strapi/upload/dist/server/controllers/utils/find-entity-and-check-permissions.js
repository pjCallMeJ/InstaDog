'use strict';

var _ = require('lodash');
var utils = require('@strapi/utils');
var index = require('../../utils/index.js');

function _interopDefault (e) { return e && e.__esModule ? e : { default: e }; }

var ___default = /*#__PURE__*/_interopDefault(_);

/**
 * Loads a file and enforces the permission *conditions* attached to `action` on it.
 *
 * `createdBy` and the creator's `roles` have to be populated before `toSubject`: the admin
 * conditions read them (`admin::is-creator` matches `createdBy.id`, `admin::has-same-role-as-creator`
 * matches `createdBy.roles`), and a subject missing those fields can never satisfy the rule — so
 * a conditioned grant would be denied its own files rather than allowed them.
 *
 * `strapiInstance` defaults to the ambient global for the admin controllers, which have no
 * `strapi` in scope. Callers that are handed an instance — MCP tool handlers — pass it
 * explicitly so they never bind to a different one.
 */ const findEntityAndCheckPermissions = async (ability, action, model, id, strapiInstance = strapi)=>{
    const file = await index.getService('upload', strapiInstance).findOne(id, [
        utils.contentTypes.constants.CREATED_BY_ATTRIBUTE,
        'folder'
    ]);
    if (___default.default.isNil(file)) {
        throw new utils.errors.NotFoundError();
    }
    const pm = strapiInstance.service('admin::permission').createPermissionsManager({
        ability,
        action,
        model
    });
    const creatorId = ___default.default.get(file, [
        utils.contentTypes.constants.CREATED_BY_ATTRIBUTE,
        'id'
    ]);
    const author = creatorId ? await strapiInstance.service('admin::user').findOne(creatorId, [
        'roles'
    ]) : null;
    const fileWithRoles = ___default.default.set(___default.default.cloneDeep(file), 'createdBy', author);
    if (pm.ability.cannot(pm.action, pm.toSubject(fileWithRoles))) {
        throw new utils.errors.ForbiddenError();
    }
    return {
        pm,
        file
    };
};

exports.findEntityAndCheckPermissions = findEntityAndCheckPermissions;
//# sourceMappingURL=find-entity-and-check-permissions.js.map
